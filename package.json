{
  "name": "application-name",
  "version": "0.0.1",
  "scripts": {
    "start": "node app/app.js",
    "importdata": "node app/importBimObjects.js",
    "typings": "typings install express --save --ambient &&  typings install serve-static --save --ambient && typings install express-serve-static-core --save --ambient && typings install mime --save --ambient && typings install node --save --ambient && typings install body-parser --save --ambient && typings install mongoose --save --ambient",
    "compile": "tsc -w"
  },
  "dependencies": {
    "body-parser": "^1.15.0",
    "express": "^4.13.4",
    "mongoose": "^4.4.10",
    "uuid": "^2.0.1"
  },
  "devDependencies": {
    "tslint": "^3.6.0",
    "typescript": "^1.8.9",
    "typings": "^0.7.9"
  }
}